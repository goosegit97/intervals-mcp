---
# Connectivity smoke test. Run after filling in inventory.ini:
#   ansible all -m ping
# or:
#   ansible-playbook ping.yml
- name: Verify connectivity to the intervals_mcp box
  hosts: intervals_mcp
  gather_facts: false
  tasks:
    - name: Ping
      ansible.builtin.ping:
