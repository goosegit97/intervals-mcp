# Ansible Galaxy dependencies. Install with:
#   ansible-galaxy collection install -r requirements.yml -p collections
collections:
  # Hardening is never hand-rolled — use the DevSec baselines (plan §1.3).
  - name: devsec.hardening
    version: ">=10.0.0"
  # ufw, sysctl helpers, etc.
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
